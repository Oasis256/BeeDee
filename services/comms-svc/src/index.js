"""
Elixir Phoenix scaffold for comms-svc
"""
# This is a placeholder. Actual Phoenix app should be generated with `mix phx.new comms_svc --no-ecto --no-html`
# Add a health endpoint in the router:
# get "/health", HealthController, :index
