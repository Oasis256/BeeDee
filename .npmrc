# Suppress engine warnings for Docker builds
engine-strict=false

# Use npm cache for faster builds
cache=/tmp/.npm

# Suppress audit warnings during CI/CD
audit=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true
