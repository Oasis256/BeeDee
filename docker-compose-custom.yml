version: '3.8'

services:
  # Backend API service
  backend:
    build:
      context: https://github.com/Oasis256/BeeDee.git
      dockerfile: Dockerfile.backend
    container_name: BeeDee-Backend
    hostname: beedee-backend
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    networks:
      default:
      macNet:
        ipv4_address: 10.0.0.32

  # Frontend service
  beedee:
    env_file:
      - ~/sVault/Prog/swag/Env/.env
    container_name: BeeDee
    hostname: beedee
    build:
      context: https://github.com/Oasis256/BeeDee.git
      dockerfile: Dockerfile
    ports:
      - "1919:80"  # Frontend
    environment:
      - NODE_ENV=production
      - VITE_API_URL=http://beedee-backend:3001
    restart: unless-stopped
    depends_on:
      - backend
    profiles:
      - prod-custom
    networks:
      default:
      macNet:
        ipv4_address: 10.0.0.31
      # shuleNet:
      #   ipv4_address: 172.16.17.50

  # Development service (separate for hot reload)
  dee-dev:
    env_file:
      - ~/sVault/Prog/swag/Env/.env
    container_name: BeeDee-Dev
    hostname: beedee-dev
    build:
      context: https://github.com/Oasis256/BeeDee.git
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
      - VITE_API_URL=http://beedee-backend:3001
    command: npm run dev:full
    depends_on:
      - backend
    profiles:
      - dev

networks:
  default:
    name: bdsm-compatibility-network
  macNet:
    external: true
